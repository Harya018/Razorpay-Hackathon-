# Genuinely separate from the backend's DB file — this is the actual
# service boundary the architecture argues for, not cosmetic.
DATABASE_URL=sqlite:///./policy_gate.db

# Used to mint approval_token (HMAC-style: session_id + final terms + this
# secret). Generate a real random value, e.g. `openssl rand -hex 32` —
# never commit a real secret.
GATE_SECRET=change-me-to-a-real-random-secret

PORT=8001
